let numeros = new Array();
numeros = [1, 2, 3, 7, 10, 12, 15, 16, 20, 80, 50, 23, 5665 ];

for(let i=0; i<numeros.length; i++ /*i=i+2 i=i+1 */)
{
    document.write(`${numeros[i]} <br/>`);

    //esta es otra forma de imprimir los arreglos
    /* const numero = numeros[i];
    document.write(numero + '<br>'); */

}

